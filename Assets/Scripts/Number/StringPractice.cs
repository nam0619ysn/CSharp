using UnityEngine;

public class StringPractice : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int number = 3;
        string result = "È¦¼ö";

        Debug.Log($"{number}Àº(´Â){result}´Ù.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
