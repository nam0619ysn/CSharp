using UnityEngine;

public class DigitSeperateNumbers : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int num = 1_000_000;
        Debug.Log("num:"+num);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
