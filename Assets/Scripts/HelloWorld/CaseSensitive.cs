using UnityEngine;

public class CaseSensitive : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("C#은 대무자 소문자 구분");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
