using UnityEngine;

public class SqureClass : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        string name = Squae.GetName();
        Debug.Log(name);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
