using UnityEngine;

public class TermPractise : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       Debug.Log("UnityEngin네임스페이스");
       Debug.Log("DebugLog");
       Debug.Log("Log 메서드");
    }

  
    /* 
    [output]

     */
}
