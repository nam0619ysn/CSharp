using UnityEngine;

public class Qutoes : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("dfsf");
        Debug.Log('n');
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
