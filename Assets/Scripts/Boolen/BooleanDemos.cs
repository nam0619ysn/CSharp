using UnityEngine;

public class BooleanDemos : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        bool bln = true;
        Debug.Log("bln:"+bln);

        bool isOut = true;
        Debug.Log("isOut:" + isOut);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
