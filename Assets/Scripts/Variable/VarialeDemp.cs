using UnityEngine;

public class VarialeDemp : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int num =7;
        Debug.Log(num);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
