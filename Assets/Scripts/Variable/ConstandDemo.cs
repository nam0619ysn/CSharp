using UnityEngine;
using UnityEngine.Rendering;

public class ConstandDemo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        const int MAX = 100;
        Debug.Log("�ִ밪:" + MAX);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
