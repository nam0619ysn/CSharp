using UnityEngine;

public class VarialbNote : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int i;

        i = 1234;

        Debug.Log(i);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
